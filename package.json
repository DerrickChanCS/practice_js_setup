{
    "name": "example-nx-workspace",
    "version": "0.0.0",
    "license": "MIT",
    "scripts": {
        "build": "nx run-many --target=build",
        "build-fresh": "nx run-many --target=build --skip-nx-cache",
        "lint": "npx eslint ."
    },
    "private": true,
    "type": "module",
    "devDependencies": {
        "base-tsconfig": "*",
        "eslint": "^8.37.0",
        "eslint-config-custom": "*",
        "nx": "15.9.0",
        "prettier": "^2.8.7",
        "typescript": "^5.0.3"
    },
    "workspaces": [
        "apps/*",
        "packages/*"
    ]
}
